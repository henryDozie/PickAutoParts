<h2><%= @post.title %></h2>
<p><%= @post.body %></p>
<hr>

<%= link_to "Post List", user_posts_path(@user) %>
<%= link_to "Edit Post", edit_user_post_path(@user, @post) %>
<%= form_for [@user, @post], html: {method: "delete"} do |f| %>
  <%= f.submit "Delete Post" %>
<% end %>